__pycache__/
*.pyc
.venv/
# experiment outputs — regenerable, never committed
runs/
scores/
# model weights / HF cache — never committed
.hf_cache/
*.safetensors
*.bin
# PII (tax return). Repo is PRIVATE; fixtures/ (derived gold + page images) is
# tracked here. sample.pdf (the raw scan) stays out unless intentionally added.
sample.pdf
