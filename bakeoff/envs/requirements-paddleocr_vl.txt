# paddlepaddle-gpu is installed in the Dockerfile from Paddle's CUDA index.
# Needs the version that exposes the `PaddleOCRVL` class (>=3.6 ships VL-1.6);
# 3.2.0 predates it -> ImportError. VERIFY-ON-VM: pin exact once confirmed.
paddleocr>=3.6.0
